# gtrace GUI 開発ノート

[GUI_Development_PLan.md](GUI_Development_PLan.md) に挙げた要件を満たすGUIの開発ノート。実装方式の比較検討、設計方針、および実装の記録をまとめる。実装の進捗は末尾の「実装記録」に追記していく。

## 実装方式の比較検討

## 要件（再掲）

1. 専用GUIで光線追跡結果を手軽に可視化できること（現状はDXFをCADソフトで開く必要がある）
2. ズーム・パンが可能なこと
3. ビーム上の任意の位置をクリックすると、そこでのビームパラメータ（q、ビーム半径、ROCなど）が表示されること
4. Windows / Linux / Mac のクロスプラットフォームで動くこと（ブラウザで動かせるか？も検討事項）

加えて、暗黙の要件として「パラメータ調整→再追跡→表示更新」の反復ループの摩擦を最小にすること、将来的には光学素子のドラッグやスライダによる対話的操作へ拡張できることが望ましい。

## 前提となる技術的事実（コード調査より）

方式選定を大きく左右する、gtraceの構造に関する事実を先にまとめる。

### 描画の抽象化は既に存在する

`gtrace/draw/draw.py` には、バックエンド非依存のシーングラフ（`Canvas` → `Layer` → `Line` / `PolyLine` / `Rectangle` / `Circle` / `Arc` / `Text`、座標はメートル単位）が定義されている。唯一のレンダラである `renderDXF()`（`gtrace/draw/renderer.py`）は、このシーングラフを約35行のisinstance分岐でDXFエンティティに変換しているに過ぎない。

**つまり、新しい表示方式の追加は「レンダラを1つ追加する」だけで済み、gtrace本体の設計変更は不要である。**

### クリック読み出しは単純な複素数演算

各 `GaussianBeam` は直線セグメント（`pos` + `dirVect` + `length`）であり、`pos` におけるqパラメータ（`qx`, `qy`）を保持している。クリック点でのパラメータは、

1. クリック点をセグメントに射影して距離 d を求める
2. `q' = q + d` を計算する
3. `w = sqrt(-2/(k·Im(1/q')))`, `R = 1/Re(1/q')` を評価する

という数行の複素数演算で得られる。この計算はPython側でもJavaScript側でも（約20行で）実装できるため、**フロントエンドの選択がこの中核機能を制約することはない**。

ただし、この読み出しにはシーングラフ（描画図形）に加えて元の `GaussianBeam` のリスト（qデータ）が必要である。**DXFファイルにはこのメタデータが一切含まれないため、書き出したDXFを表示する方式（tutorialで実験したdxf-viewerの延長）はデッドエンドである。**

### その他の制約

- 依存パッケージは numpy / scipy / traits。`traits` はC拡張であり、Pyodide（ブラウザ内でPythonを完全実行する方式）は実質的にブロックされる。
- Tutorialノートブックでのdxf-viewer実験では、CDN経由のESMモジュール解決とJupyterサーバーへの依存で苦戦した。**外部JSパッケージへの依存は避け、自己完結なビューアを書くべき**という教訓が得られている。

## 各方式の評価

### A. matplotlib 対話的Figure

`renderMatplotlib(canvas, ax)` を追加し、pickイベントでクリック読み出しを行う方式。

- **長所**: 追加依存が実質ゼロ（科学計算環境にはほぼ必ず入っている）。実装1〜2日。スクリプトでもノートブックでも動く。10年後も動く安心感。
- **短所**: ズーム/パンがツールバーモード式でUXが弱い（ホイールでカーソル中心ズームするには自前実装が必要）。ノートブックでの対話表示はipympl依存で、これはJupyterスタックの中でも壊れやすい部分。将来のドラッグ操作・ライブ再追跡はmatplotlibのイベントモデルでは非常に苦しい。
- **評価**: 「クイックルック用の簡易レンダラ」としては優秀だが、本命GUIには力不足。

### B. ネイティブデスクトップGUI（PySide6 / Qt）

QGraphicsSceneベースのデスクトップアプリ。

- **長所**: 操作性は全方式中で最高。アイテム単位のヒットテスト、滑らかなズーム、素子のドラッグがほぼ標準機能。
- **短所**: 約180MBの重い依存。プラットフォームごとのパッケージング摩擦（Linux/Wayland、macOS署名等）。Jupyterとの相性が悪い（Qtイベントループとカーネルの衝突、リモートJupyterでは表示不可）。単独開発の科学プロジェクトとしては保守負担が最大。「ブラウザで動くか」への答えにならない。
- **評価**: 操作性は魅力だが、配布・保守の観点でこのプロジェクトには不適。

### C. ローカルWebアプリ（Pythonサーバ + JSビューア）

Python側で `Canvas` とビームデータをJSONにシリアライズし、標準ライブラリのHTTPサーバで小さなHTML/JSビューアを配信。ズーム/パン/ヒットテストはJS側で実装。

- **長所**: 「ブラウザで動くか？」に正面から答える方式。ブラウザ自体がGUIツールキットなので3 OSで完全に同一動作。標準ライブラリだけで実装可能（追加依存ゼロ）。**反復ループが全方式中最良**: ブラウザタブを開いたまま追跡スクリプトを再実行すると、websocket/SSE経由で表示がライブ更新される。SSHポートフォワードで実験室マシン上の結果をローカルで見る使い方も可能（KAGRAでの実運用に合致）。将来の「ミラーをドラッグ→Python側で再追跡→push更新」への発展経路が素直。
- **短所**: 依存なしのJSビューア（ズーム/パン変換、ヒットテスト、読み出しパネル。現実的に500〜1000行）を書いて保守する必要がある。サーバプロセスのライフサイクル管理（ポート衝突等）。
- **評価**: 総合力で最有力。ただし一度に作るには工数がやや大きい（1〜2週間）。

### D. 自己完結HTML出力

`renderHTML(canvas, beams, 'trace.html')` で、JSONデータとインラインJSビューアを埋め込んだ静的HTMLファイルを1枚書き出す方式。ブラウザで開き、再追跡後はF5で更新。

- **長所**: **現在のDXFワークフローと同じメンタルモデル**（`renderDXF` を `renderHTML` に置き換えるだけ）で、CADソフトが不要になる。サーバ不要・追加依存ゼロ・インストール不要。クリック読み出しを含む全ビューア機能がファイル単体で動作する。共同研究者にファイルを送るだけで共有でき、静的HTMLなので10年後も開ける。工数3〜5日で日常の苦痛の約8割を解消できる、**費用対効果が最も高い方式**。
- **短所**: ライブpushはない（F5または自動リロードタイマーで代替。実用上は小さな差）。表示専用であり、素子のドラッグ→再追跡は原理的に不可能（Python側が存在しないため）。
- **評価**: 最初に作るべき成果物として理想的。JSビューアコアはC・Eにそのまま流用できる。

### E. Jupyterウィジェット（anywidget）

同じJSビューアコアをanywidgetでラップし、ノートブックのセル出力として埋め込む方式。

- **長所**: Tutorialで実証済みのノートブックワークフローに最適。anywidgetは、dxf-viewer実験で苦戦したESMモジュール解決問題をまさに解決するために作られた仕組みで、JupyterLab / Notebook / VS Code / Colabで動く。カーネルが生きているため、Python↔JS双方向通信による対話操作（ドラッグ→再追跡）の実験台としては最も安価。
- **短所**: ノートブック専用で、スクリプトユーザーには何も提供しない。`anywidget` + `ipywidgets` の依存が増える。
- **評価**: 単独では不十分だが、共有JSコアの薄いラッパーとしては安価で価値が高い。

### F. Plotly / Bokeh / Panel / Dash

既製の対話的プロットフレームワークを使う方式。

- **短所が本質的**: これらのクリック/ホバーイベントは「プロットされた頂点」単位であり、**「ビーム上の連続的な任意点」をクリックしてその場のqを計算するという中核要件と衝突する**。回避するにはカスタムJSを書くことになり、結局JSを書くならフレームワークの外で書いた方が自由度が高い。ビーム包絡線のレイヤ別描画もフレームワークと相性が悪い。依存も重く（各〜50MB級）、過去にメジャーバージョン間でAPIが壊れた履歴がある。
- **評価**: 「無料のズーム/パン」のために最重要機能の制御を手放すことになる。非推奨。

### G. Pyodide（ブラウザ内でPythonを完全実行）

- `traits` がC拡張でありPyodideホイールが存在しないため、実質的にブロックされている。scipyのロードも重い。将来gtraceからtraits依存を外す近代化を行えば再検討可能だが、GUI選定の判断材料にすべきではない。
- **評価**: 非推奨。なお「Pythonはネイティブで動かし、ビューアだけブラウザ」という C / D が、実用的な意味での「ブラウザで動くgtrace」である。

## 比較表

| 基準 | A matplotlib | B Qt | C ローカルWebアプリ | D 静的HTML | E anywidget | F Plotly等 | G Pyodide |
|---|---|---|---|---|---|---|---|
| 可視化品質 | ○ | ◎ | ◎ | ◎ | ◎ | ○ | — |
| ズーム/パン | △ ツールバー式 | ◎ | ◎ | ◎ | ◎ | ◎ | — |
| 任意点クリック読み出し | ○ | ◎ | ◎ | ◎ | ◎ | △ 頂点単位 | — |
| クロスプラットフォーム+ブラウザ | ○ (ブラウザ不可) | △ | ◎ | ◎ | ◎ (要Jupyter) | ○ | ✕ |
| 反復ループ (再追跡→表示) | △ 再実行 | △ | ◎ ライブpush | ○ F5/自動更新 | ◎ セル再実行 | ○ | — |
| 実装工数 | ◎ 1-2日 | ✕ 数週間 | △ 1-2週間 | ○ 3-5日 | ○ D+1-2日 | ○ | ✕ |
| 追加依存 | ◎ mplのみ | ✕ ~180MB | ◎ stdlib | ◎ なし | ○ anywidget | △ 重い | ✕ |
| ノートブック対応 | △ ipympl | ✕ | ○ | ○ | ◎ | ○ | — |
| スクリプト対応 | ◎ | ◎ | ◎ | ◎ | ✕ | ○ | — |
| 将来のドラッグ/ライブ再追跡 | △ | ◎ | ◎ | ✕ 表示専用 | ◎ | △ | — |
| 長期保守性 (単独開発) | ◎ | △ | ○ | ◎ | ○ | △ API変動 | ✕ |

（◎ 優 / ○ 良 / △ 弱 / ✕ 不可・不適）

## 推奨: 依存ゼロのJSビューアコアを1つ作り、D → E → C の3段階で提供する

C・D・Eの3方式は、JSビューアコアとPython側シリアライザを約90%共有できる。したがってこれは「3つのプロジェクト」ではなく、**「1つのコア + 薄いアダプタ×3」**であり、各段階が単独で出荷可能かつ有用である。

推奨理由:

1. ブラウザは、3 OS対応・「ブラウザで動くか？」への肯定的な回答・追加インストールゼロ・中核機能（任意点クリック読み出し）の完全な制御、を同時に満たす唯一のフロントエンドである。
2. dxf-viewer実験の教訓（外部JSパッケージとDXFという媒体が問題だった）を、自作の依存ゼロビューア + gtrace自身のシーングラフ/ビームJSONで正面から解決する。
3. Stage 1（静的HTML）だけでも、CAD不要・即時更新・q読み出し・共有可能ファイルという形で日常の苦痛の大半を解消できる。仮に後続ステージを作らなくても、Stage 1は恒久的に有用であり続ける。

### ロードマップ

- **Stage 0 — シリアライザ整備**（半日、挙動変更なし）
  - `gtrace/draw/serialize.py` を新規作成: `canvas_to_dict(canvas)`（`renderDXF` のisinstance分岐をJSON化に写像）と `beams_to_dict(beamList)`（端点・`dirVect`・`length`・`qx`/`qy`・`wl`・`n`・`P`・`optDist`・Gouy位相・`stray_order`・`name` を出力。DXFには存在しなかったメタデータチャネル）
  - `gtrace/draw/tools.py` の既存バグ（`drawOptSys` 内の `render.renderDXF` → NameError）の修正と、レンダラのプラガブル化
- **Stage 1 — 静的HTML出力（方式D）**: `renderHTML(canvas, beams, filename)`。埋め込みJSON + インラインJS（SVGベース。ホイールでカーソル中心ズーム、ドラッグでパン、最近傍セグメントのヒットテスト、読み出しパネルに w(x/y)・R(x/y)・q・ウェスト位置・Gouy位相・パワーを表示）。既存スクリプトは1行の変更で移行できる。
- **Stage 2 — anywidget（方式E）**: 同じviewer.jsをESMモジュール化してノートブックに埋め込む。セル再実行でその場更新。
- **Stage 3 — ライブサーバ（方式C）**: 標準ライブラリのHTTPサーバ + SSE/websocketによるpush更新。将来の「ミラーをドラッグ→再追跡」はここに実装する（`Mirror` のtraits属性は相互整合的に更新されるため、`center` や `normAngleHR` を書き換えるだけで幾何が追従し、ドラッグ操作に向いている）。

### 却下した方式

- **B (Qt)**: 依存の重さ・ノートブック非対応・単独保守の負担が、操作性の利点に見合わない。
- **F (Plotly/Bokeh等)**: 中核機能（任意点読み出し）とフレームワークが衝突する。
- **G (Pyodide)**: `traits` のC拡張依存でブロック。traitsを外す近代化を行った場合にのみ再検討。

## 双方向化と OpticalLayout クラスの検討

将来の拡張として、GUI内で光学素子を動かしたり追加したりでき、その操作がPython側の光学モデルに伝わる双方向性を持たせたい。この要件が方式選定と設計に与える影響を検討する。

### 双方向性を前提とした方式選定

双方向性にはGUIの背後で生きているPythonプロセスが必須である。したがって:

- **静的HTML (D)** はこの用途では原理的に不可能（表示専用の共有・アーカイブ用として価値は残る）
- **anywidget (E)**: カーネルが常に生きているため、Python↔JS双方向通信（traitlets同期）が追加インフラゼロで手に入る。双方向機能の開発・実験の場としては最安
- **ローカルWebアプリ (C)**: websocket/SSEで同等の双方向性を実現。スクリプトユーザー・単独アプリ用途はこちら

EとCでは「GUI操作→Python」の**メッセージプロトコルを共通化**できる。例:

```json
{"op": "move", "target": "M1", "center": [0.1, 0.25]}
{"op": "add",  "type": "Mirror", "params": {"diameter": 0.1}}
```

のようなJSONメッセージを定義しておけば、輸送層（anywidgetのtraitlets か websocket か）だけが異なる同一実装になる。つまり推奨戦略（共有JSコア + 薄いアダプタ）は双方向化後も崩れず、**Stage 2 (anywidget) の優先度が上がる**。

### OpticalLayout クラス

GUI操作の受け皿として、光学系全体を表すモデルクラス（MVCのModel相当）を新設する。

**必要性**: 現状のgtraceには「光学系全体」を表すオブジェクトが存在しない。ミラーも光源ビームもユーザースクリプトのローカル変数であり、追跡ルール（`order`, `power_threshold`）は `non_seq_trace()` の引数として毎回渡される。この状態では、GUIから「M1を動かせ」というメッセージが来ても宛先を解決する場所がない。要素に安定したIDを持ち、追跡を自分で再実行できる単一のオブジェクトが必要である。

**2フェーズのワークフロー**: 逐次追跡と非逐次追跡の役割を次のように分担する。

1. **構築・アラインメントフェーズ** = Pythonコード。逐次追跡（`hitFromHR` の鎖）や共振器固有モードの計算はここで行う。この作業はGUIに載せる必要がない（載せられない）。
2. **登録後（OpticalLayout）** = 非逐次追跡（`non_seq_trace`）のみ。1つまたは少数の光源ビームを定義し、そこから追跡する。可視化・パラメータ微調整・迷光確認のフェーズであり、GUIと1対1対応するのはここだけ。

この分割により「逐次追跡は宣言的ルールとして表現しにくい」という問題が設計から消える。

**追跡ルール**: 無駄なビームの追跡を防ぐため、以下をルールとしてlayoutに持たせる。

- レイアウト既定の内部反射次数 `order`・`power_threshold`・`open_beam_length`
- 素子ごとの `order` オーバーライド（`Mirror.hit(beam, order, threshold)` は呼び出しごとに `order` を受け取る作りなので、`non_seq_trace` を「素子ごとの辞書を参照する」よう拡張するのは小さな変更で済む）
- なお `Mirror` には `term_on_HR` / `term_on_HR_order`（HR面でビームを終端させる）が既にあり、素子単位ルールの前例になっている

**設計上のポイント**:

- **薄いコンテナに留める**: 既存の `Mirror` / `GaussianBeam` / `non_seq_trace` をそのまま保持・呼び出すラッパーとし、追跡ロジックを再実装しない。既存の関数型APIは壊さず、OpticalLayoutは任意利用とする。
- **一意なIDの強制**: layoutへの追加時に名前の重複チェック（またはID自動付与）を行い、GUIメッセージの宛先解決を保証する。
- **参照で持つ**: 登録された素子はコピーせず参照で保持する。GUIでM1を動かす → layout内のM1（=ユーザーのM1と同一オブジェクト）のtraits属性が書き換わる → Python側から見ても値が一致する、という一貫性を保つ。`Mirror` の幾何属性はtraitsにより相互整合的に更新されるため、「ドラッグ→属性1つ書き換え→再追跡」がそのまま成立する。
- **1対1対応の範囲**: GUIと1対1対応させるのは編集可能な要素（光学素子・光源・追跡ルール）だけ。ビーム（追跡結果）は派生データであり、対応の向きは一方向に固定する:

  ```text
  GUI編集 → OpticalLayout変更 → layout.trace() → Canvas/ビームJSON → GUI再描画
  ```

- **光源の扱いの注意点**: アラインメントフェーズで決めた光源のq（例えば共振器固有モード）は、その時点の幾何に対する解である。GUIで素子を動かした後も光源は固定のままなので、共振器系では「もはや固有モードでないビーム」を追跡することになる。迷光確認・レイアウト検討では正しい挙動だが、将来「素子移動後に光源を再計算するフック」をオプションで持たせる余地を残す（初期実装では不要）。
- 光源はリストで持ち、ソースごとにレイヤ/色を割り当てられるようにする（メインビーム + ピックオフ等の複数入射に対応）。

**APIイメージ**:

```python
# 構築フェーズ(従来通りのPythonコード、逐次追跡でアラインメント)
M1, M2, M3 = ...  # 配置・角度合わせ

# 登録
layout = OpticalLayout(
    optics=[M1, M2, M3],
    sources=[b0],
    rules=TraceRules(order=5, power_threshold=1e-6,
                     per_optic_order={"M3": 1})
)
layout.trace()      # non_seq_traceを実行、結果を保持
layout.show()       # GUI表示(将来は双方向)
```

**副次的な利点**: layoutが直列化可能になると、(a) 光学系のファイル保存/読み込み（YAML/JSON形式のレイアウトファイル）、(b) GUIでのundo/redo（layout操作のコマンド履歴）、(c) `layout.trace()` / `layout.show()` という簡潔なAPI、が全て手に入る。GUIと無関係にライブラリとしての価値が上がる。

### ロードマップへの反映

OpticalLayoutはStage 0のシリアライザ設計に影響する（シーングラフJSONに加えて「レイアウトJSON」のチャネルが増える）ため、次のように組み込む:

- **Stage 0.5 — OpticalLayout 追加**: 素子リスト・光源・追跡ルール・`trace()`・`to_dict()`/`from_dict()`。GUIなしでも単体で有用
- Stage 1（静的HTML）はlayoutの有無に関係なく従来通り動作
- Stage 2（anywidget）で編集メッセージプロトコルを導入し、OpticalLayoutを宛先として双方向化
- Stage 3（ライブサーバ）は同じプロトコルをwebsocketに載せ替える

## 実装記録

新しい日付のエントリを上に追記していく。

### 2026-08-01: Stage 0 — シリアライザ整備

**実装内容**:

- `gtrace/draw/serialize.py` を新規作成
  - `canvas_to_dict(canvas)`: `renderDXF` と同じisinstance分岐で `Canvas` をJSON互換辞書に変換。DXFと違いスケーリングせずメートル単位のまま。構造は `{unit, layers: [{name, color, shapes: [...]}]}`。全6種のShape（line / polyline / rectangle / circle / arc / text）に対応
  - `beam_to_dict(b)` / `beams_to_dict(beamList)`: ビームの物理パラメータ（`pos`・`end`・`dirVect`・`dirAngle`・`length`・`qx`/`qy`・`wl`・`n`・`P`・`wx`/`wy`・`Gouyx`/`Gouyy`・`optDist`・`stray_order`・`name`・`layer`）をJSON互換辞書へ。複素数は `[実部, 虚部]` ペアで表現。DXFに存在しなかったメタデータチャネル
  - `scene_to_dict(canvas, beams)`: ビューアが読む最上位構造 `{canvas, beams}`
- `gtrace/draw/tools.py` の修正
  - `drawOptSys` の既存バグ（`render.renderDXF` — importは `renderer` 名なのでNameError）を修正
  - `render_func` 引数を追加してレンダラをプラガブル化（既定は従来通り `renderer.renderDXF`）

**検証（全14項目 PASS）**:

- 3ミラー系の `non_seq_trace` 結果をシリアライズし、JSONラウンドトリップ成功
- 物理値の照合: ビューアがJS側で使う式 `q' = q + d` → `w = √(-2/(k·Im(1/q')))`（k = 2πn/λ）, `R = 1/Re(1/q')` の結果が、gtraceの `beam.width(d)` / `gauss.q2R()` と相対誤差ゼロで一致
- `q + d` が `beam.propagate(d)` の結果と一致
- `drawOptSys` がデフォルト（DXF）とカスタム `render_func`（JSON）の両方で動作
- `renderDXF` の回帰なし（`tests/test_gtrace.py` 正常終了）
