# gtrace用GUIの実装

## 現状
gtraceは、pythonコードで光学システムをオブジェクトとして構築し、光線追跡した結果を、DXFファイルへと書き出す形になっている。結果を確認するには、CADソフトなどでDXFファイルを開かなければならない。また、レンズの位置などを調節した後の変化を見るには、一旦コードに戻って、光線追跡とDXFへの書き出しを再度行い、CADソフトで開き直すという面倒なステップを踏む。

## やりたいこと
- 専用のGUIで、簡単に光線追跡結果を可視化して見ることができるようにしたい。
- ズームやパンも可能にする。
- ビームの任意の位置をクリックすると、そこでのビームパラメータ(qやビーム半径、ROCなど)が表示されるようにしたい。
- Windows, Linux, Macのクロスプラットフォームで動くようにしたい。
    - Webブラウザで動くようにできるか?
